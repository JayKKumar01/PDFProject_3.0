public class Notes {
    //frame name, default path
    // add the features
    // fix the wrong path getting saved in the properties file
    // appsettings needs color current color operations set by user
    // pause the console till word conversion
}
